#include<iostream>
#include<vector>
using namespace std;
int usingRec(vector<int>&nums,int n){
	if(n==0) return 0;
}
int houseRobbery(vector<int>&nums){
	int n=nums.size();
	return usingRec(nums,n);
}
//same as maximum sum of adjecent element but catch is we if take first element so we can't take last elemnt
int main(){
	return 0;
}